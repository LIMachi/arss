{
  "schemaVersion": 1,
  "id": "${mod_id}",
  "version": "${version}",
  "name": "${readable_name}",
  "description": "${description}",
  "authors": [
    "${author}"
  ],
  "contact": {
    "homepage": "",
    "sources": ""
  },
  "license": "${license}",
  "icon": "assets/${mod_id}/icon.png",
  "environment": "*",
  "entrypoints": {
    "main": [
      "${maven_group}.fabric.MainFabric"
    ],
    "client": [
      "${maven_group}.fabric.MainFabric"
    ]
  },
  "mixins": [
    "${mod_id}.mixins.json",
    "${mod_id}-fabric.mixins.json"
  ],
  "depends": {
    "fabricloader": ">=0.16.0",
    "minecraft": "~${minecraft_version}",
    "java": ">=21",
    "architectury": ">=13.0.2",
    "fabric-api": "*",
    "lim_lib": ">=${lim_lib_version}"
  },
  "suggests": {
    "another-mod": "*"
  },
  "accessWidener": "${mod_id}.accesswidener"
}
